# Playwright flows

Implemented in task 21.10: sign up through verification into the product; ask a question then open
its evidence; save a location then see the unit preference applied; and an expired session routed to
sign-in. Vitest is configured to exclude this directory.
