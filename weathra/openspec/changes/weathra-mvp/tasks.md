# Part A — MVP / capstone implementation

Tracked work for this change. Ordered by dependency: foundation and identity first, then the data and analytics spine, then the tool boundary, then orchestration, then the API, then design, then the frontend, then evaluation and delivery.

Authentication lands early (group 4) because memory, the API, the graph, and every frontend screen depend on having a validated principal to scope against. The Visily design phase (group 19) completes before substantial frontend implementation begins, seeded by the design direction already approved during the earlier UXPilot exploration rather than starting from nothing.

Groups 26–34 add the SaaS-ready model policy layer. They come after the orchestration and API spine because every one of them plugs into a seam that spine already establishes — the `LLMClient` Protocol, the settings layer, the migration and Row Level Security pattern, the retention routine, and the evaluation runner. No group below changes the locked architecture: React/Next.js → FastAPI → LangGraph → Forecast/Historical/Analytics/RAG → MCP → Open-Meteo, on PostgreSQL/Supabase, with both memory tiers, deterministic analytics outside the model, and a provider-agnostic `LLMClient`, all stand exactly as specified in groups 1–25.

## 1. Repository and tooling foundation

- [x] 1.1 Create the monorepo skeleton (`backend/`, `frontend/`, `docs/`, `.github/workflows/`) with the backend package layout of design.md decision 1; verify every backend package imports cleanly and `docs/` contains a placeholder for each documentation deliverable named in group 24.
- [x] 1.2 Create `backend/pyproject.toml` targeting Python 3.12 with runtime dependencies (fastapi, uvicorn, httpx, pydantic, pydantic-settings, langgraph, langgraph-checkpoint-postgres, mcp, sqlalchemy, asyncpg, alembic, pgvector, fastembed, numpy, a JOSE/JWT library for token validation) and a `dev` extra (pytest, pytest-asyncio, respx, ruff); verify `pip install -e ".[dev]"` succeeds and `python -c "import weathra"` runs.
- [x] 1.3 Assert that `anthropic` and every model-vendor SDK are absent from backend dependencies, and add a test that fails if any is introduced; verify the test passes and the dependency tree contains no vendor SDK.
- [x] 1.4 Configure ruff (lint + format), mypy or pyright, and pytest (asyncio mode, `live` and `db` markers deselected by default) for the backend; verify all three run clean on the empty tree.
- [x] 1.5 Implement `backend/weathra/config.py` as a pydantic-settings `Settings` covering every backend variable in design.md decision 19's table, with a validator refusing to start when the service-role key is present on the request-serving path; verify a unit test constructs `Settings` with only the required variables and asserts the documented defaults, that no LLM credential is required to construct it, and that the validator rejects the misconfiguration.
- [x] 1.6 Write `backend/.env.example` and `frontend/.env.example` documenting every variable with the public/secret split of design.md decision 19; verify a test asserts no variable carrying a secret uses a `NEXT_PUBLIC_` prefix and that both files list every variable the code reads.
- [x] 1.7 Add the architecture-boundary test enforcing design.md decision 1's import rule — nothing below `agents/` imports `agents` or an LLM client, nothing in `agents/nodes/` imports a provider client directly, and only `api/` and `memory/` import `auth/`; verify the test passes and fails when a deliberate violating import is added.
- [x] 1.8 Scaffold the Next.js + TypeScript frontend with lint, type-check, and test tooling, the Supabase JavaScript client and its SSR helpers, and `frontend/.env.example` carrying only public configuration; verify `npm run lint`, `npm run typecheck`, `npm test`, and `npm run build` all succeed with the backend not running. (Tooling scaffolding only — screen implementation waits for group 19.)

## 2. Domain models and error hierarchy

- [x] 2.1 Implement `domain/errors.py` with the `WeathraError` hierarchy and stable codes from design.md decision 16, including the authentication and authorization error classes; verify a unit test asserts each class's code and that every subclass carries a message.
- [x] 2.2 Implement `domain/location.py` — `Location` with display name, coordinates, timezone, optional region/country/elevation, and the stable coordinate-derived identifier, plus `Resolution` as `Resolved | Ambiguous`; verify unit tests cover a valid location, out-of-range latitude rejection, both resolution variants, and that resolving the same place by name and by coordinates yields the same identifier.
- [x] 2.3 Implement `domain/weather.py` — `UnitSystem`, `DataClass`, `SeriesEntry` with dual timestamps, `Series` with a series-level units map and `float | None` values, `CurrentConditions`, `Forecast`, and `HistoricalObservations`, each carrying provider, units, `retrieved_at`, `from_cache`, and `data_class`; verify unit tests assert an absent measure serializes as null rather than zero, both timestamp forms are present, and every result carries a data class.
- [x] 2.4 Implement `domain/identity.py` with the `Principal` model (auth subject, email, claims) as the only representation of an acting user; verify a unit test asserts it is constructible only from validated claim data and carries no token material.
- [x] 2.5 Implement `domain/analytics.py`, `domain/comparison.py`, and `domain/evidence.py` — statistic results with provenance, comparison candidates with component contributions, and the evidence record and response envelope of design.md decision 14; verify all models round-trip through JSON without losing a provenance field.

## 3. Database, migrations, and Row Level Security

- [x] 3.1 Implement `db/models.py` with the SQLAlchemy models for every table in design.md decision 10, each user-owned table carrying `user_id` referencing the Supabase Auth subject and each shared table carrying none; verify a unit test asserts every table's primary key and that the ownership classification matches the decision 10 table exactly.
- [x] 3.2 Configure Alembic with separate privileged and request-serving connection settings, and write the initial migration creating the schema, enabling `pgvector`, and adding the HNSW index on chunk vectors; verify the migration applies to an ephemeral Postgres and downgrades cleanly.
- [x] 3.3 Write the migration enabling Row Level Security on every user-owned table with owner-restricting policies, leaving shared tables unrestricted per design.md decision 5; verify a `db` test asserts RLS is enabled with a policy on each user-owned table and absent on each shared one.
- [x] 3.4 Implement the request-scoped session dependency that opens a transaction, sets the request's claims, and assumes the restricted role so policies apply to backend queries, plus the separate privileged engine used only by migrations and administrative routines; verify `db` tests assert a query under one user's claims cannot see another user's row even when the ownership predicate is deliberately omitted, and that the privileged connection is distinct.
- [x] 3.5 Configure CI to provide an ephemeral Postgres with pgvector for `db`-marked tests; verify `pytest -m db` passes in CI with no local database installed.

## 4. Authentication and authorization (backend)

- [x] 4.1 Implement `auth/jwks.py` — the signing-key cache fetching the project key set, refreshing on a bounded TTL, and refetching immediately on an unknown key id; verify unit tests cover a cached hit, a TTL refresh, an unknown-key-id refetch, and a fetch failure surfacing a distinguishable error without a per-request fetch.
- [x] 4.2 Implement `auth/tokens.py` validating signature, issuer, audience, and expiry with a small clock-skew leeway, returning a `Principal`; verify unit tests using a locally-minted test key pair cover a valid token, an expired token, a wrong issuer, a wrong audience, a bad signature, an unknown key id, a malformed token, and a token carrying an unverified-email claim being rejected — all with no network access.
- [x] 4.3 Implement `auth/deps.py` — the FastAPI dependency producing the `Principal` for protected routes and an optional-principal variant for public routes that apply preferences, ignoring any user, profile, thread, or session identifier arriving in a header, parameter, or body as identity; verify tests assert a valid token yields the right subject, a missing token on a protected route returns 401, and a token for one user plus a header naming another acts as the token's subject.
- [x] 4.4 Implement `auth/profiles.py` — idempotent application-profile creation keyed by the auth subject on first authenticated use, holding no credential or duplicated contact data; verify `db` tests cover creation on first use, idempotency under concurrent first requests, and that the table holds no password or token material.
- [x] 4.5 Implement `auth/rls.py` binding the principal's claims into the request-scoped database session so policies evaluate against the acting user; verify a `db` test asserts the claims are visible to a policy and that a session opened without a principal cannot read user-owned rows.
- [x] 4.6 Implement the ownership-enforcing repository helpers that constrain every user-owned read and write by the principal and return not-found rather than forbidden on a miss; verify `db` tests cover a scoped read, a scoped write, a cross-user read returning not-found, and a cross-user mutation leaving the target unchanged.
- [x] 4.7 Implement the token-material redaction rule for logs and error bodies; verify tests assert an authentication failure logs its reason without the token and returns a body containing no token or secret material.

## 5. Weather provider layer

- [x] 5.1 Implement `providers/base.py` with the `WeatherProvider` Protocol (`current`, `forecast`, `history`, `capabilities`) and the `ProviderCapabilities` descriptor reporting maximum horizon, earliest historical date, and supplied measures per granularity; verify a test asserts a stub satisfies the Protocol at runtime.
- [x] 5.2 Implement `providers/registry.py` as the explicit factory dict with lookup by name, enumeration, and `ProviderNotFound` listing available names; verify unit tests cover default lookup, named lookup, and the unknown-name error listing available names.
- [x] 5.3 Implement `providers/http.py` — the shared `httpx.AsyncClient` factory with the timeout and bounded retry/backoff policy of design.md decision 8, translating transport and status failures into `ProviderUnavailable`, `ProviderTimeout`, and `ProviderRateLimited`; verify respx tests cover a timeout, a 429, a 500 that succeeds on retry, and a non-retried 400.
- [x] 5.4 Implement `providers/open_meteo.py` current and forecast retrieval, mapping the payload field-by-field into the normalized model for both unit systems across every measure in `specs/weather-providers`, and declaring real capabilities; verify respx tests against recorded fixtures assert the normalized output, absent-measure handling, and both unit systems.
- [x] 5.5 Implement Open-Meteo historical/archive retrieval with the archive reporting lag handled per `specs/historical-weather`, returning partial coverage with a statement of what is unavailable; verify respx tests cover a fully covered range, a range hitting the reporting lag, and a range before the earliest available date.
- [x] 5.6 Implement horizon and historical-range validation against `capabilities()`, raising `UnsupportedHorizon` and `RangeOutsideCoverage` with the relevant bound, plus the 7-day default with at least 48 hours hourly; verify unit tests cover the default horizon, an over-long horizon, an inverted range, and a future-dated historical range.
- [x] 5.7 Implement `providers/cache.py` as the two-tier `CachedProvider` wrapper of design.md decision 8 with per-key lock collapsing and `retrieved_at`/`from_cache` propagation; verify unit tests cover a cache hit, TTL expiry, metric-versus-imperial keying separately, historical entries outliving forecast entries, and two concurrent identical requests producing one upstream call.
- [x] 5.8 Add a test asserting no normalized result exposes a provider-specific field name, that no module above `providers/` imports a provider client, and that the cache holds no user-identifying key material; verify the test passes.

## 6. Location resolution

- [x] 6.1 Implement `geocoding/base.py` with the `Geocoder` Protocol (`search`, `resolve`) returning `Resolution`; verify a stub satisfies the Protocol in a test.
- [x] 6.2 Implement `geocoding/open_meteo.py` against the Open-Meteo geocoding API with case and whitespace normalization, region-qualifier preference, ambiguity detection, and `LocationNotFound` for no match; verify respx tests cover a unique name, a region-qualified name, mixed casing with whitespace, an ambiguous name returning candidates, and an unknown name.
- [x] 6.3 Implement coordinate-pair resolution with range validation and timezone lookup, and geocoding result caching; verify unit tests cover valid coordinates returning a timezone, an out-of-range latitude naming the field, and a repeat resolution served from cache.
- [x] 6.4 Implement bounded location search with a default limit returning an empty list rather than an error; verify respx tests cover ranked results honouring the limit and an empty result set.

## 7. Deterministic analytics engine

- [x] 7.1 Implement `analytics/descriptive.py` — minimum, maximum, mean, and range for temperature and other continuous measures, with extreme timestamps, tie recording, and absent-value exclusion with counts; verify unit tests cover each statistic, a tied extreme, and a series containing absent values.
- [x] 7.2 Implement `analytics/precipitation.py` — window and per-day totals, wet-threshold counts, and probability analysis reported only from provider-supplied probability; verify unit tests cover totals, threshold counts, probability analysis, and a series without probability reporting it unavailable with no inferred value.
- [x] 7.3 Implement `analytics/wind.py` and humidity/pressure statistics — mean and range humidity, dew point and pressure, mean and maximum sustained wind, maximum gust with timestamp, and prevailing direction sector; verify unit tests cover each statistic and a series missing a measure reporting it unavailable while others compute.
- [x] 7.4 Implement `analytics/rolling.py` and `analytics/distribution.py` — rolling averages, period deltas, and percentiles with the interpolation method stated; verify unit tests cover a rolling mean, a signed delta, a percentile with its named method, and a rolling window longer than the series raising with both lengths.
- [x] 7.5 Implement z-scores against a supplied reference with undefined handling for zero variance; verify unit tests cover a computed z-score naming its reference period and a zero-variance reference reported undefined without raising.
- [x] 7.6 Implement `analytics/anomaly.py` using the MAD method of design.md decision 9, always reporting extremes and stating method and threshold; verify unit tests cover an outlier day, a uniform series reporting none, a zero-MAD series, and a single extreme in a seven-point series being detected rather than masked.
- [x] 7.7 Implement `analytics/trend.py` as least-squares slope classified against per-measure insignificance margins; verify unit tests cover rising, falling, steady, and a single end spike not producing a trend.
- [x] 7.8 Implement the minimum-points declarations and the structured self-describing result wrapper carrying statistic, value, unit, window, location, provider, point count, and method; verify unit tests cover partial computability with counts reported, an empty series raising, and a result round-tripping through serialization.
- [x] 7.9 Add tests asserting analytics purity — identical repeated results, and every function succeeding with no network, no database, no authentication, and no inference credential available; verify both tests pass.

## 8. Forecast and historical services

- [x] 8.1 Implement window resolution from a location's timezone to concrete UTC bounds; verify unit tests cover a non-UTC timezone and a window crossing a DST transition.
- [x] 8.2 Implement `weather/forecast_service.py` — current conditions and hourly/daily forecast retrieval across every supported measure, composing analytics results and reporting window, location, provider, units, and data class; verify tests over recorded fixtures assert the composed result, per-measure unavailability, and determinism for a fixed series.
- [x] 8.3 Implement threshold crossings reporting first and subsequent crossings in local time, explicit not-crossed results, and rejection for an unavailable measure; verify unit tests cover a crossing, a never-crossed threshold, and an unsupported-measure rejection.
- [x] 8.4 Implement `weather/uncertainty.py` — the uncertainty statement carrying horizon distance, provider-supplied spread where available, and the single-provider basis disclosure; verify unit tests assert a near-term and a far-horizon figure are qualified differently and that the basis is always disclosed.
- [x] 8.5 Implement the deterministic plain-language forecast summary with no language-model dependency, naming location and window and stating only computed figures; verify a test asserts the summary content for a known analysis and that it generates with no inference credential configured.
- [x] 8.6 Implement `weather/snapshots.py` — snapshot capture on each forecast retrieval, keyed by location and window with no user reference, non-blocking and logging on storage failure; verify `db` tests assert a snapshot is written on retrieval, carries no user column, and that the forecast still returns successfully when snapshot storage fails.
- [x] 8.7 Implement What Changed? — per-day signed deltas against the most recent earlier snapshot for the same location and window, both retrieval times, insignificance-margin handling, and an explicit no-prior-snapshot result; verify `db` tests cover a movement report, a no-prior-snapshot case, immaterial movement reported as unchanged, and one user's request benefiting from a snapshot captured for another.
- [x] 8.8 Implement `weather/history_service.py` — historical retrieval labelled as observations, period-versus-period comparison with shared basis and unequal-length statement, and the one-period-unavailable rejection; verify tests over recorded fixtures cover each case.
- [x] 8.9 Implement historical baselines over a requested number of years, reporting years actually used and labelled a Weathra-computed statistic; verify tests cover a full baseline, fewer years available than requested, and the honest labelling.
- [x] 8.10 Implement comparison against baseline with signed difference, z-score, plain-language characterization, forecast-side uncertainty statement, and zero-variance handling; verify tests cover a forecast-versus-baseline comparison, the forecast-side qualification, and a zero-variance baseline.
- [x] 8.11 Implement the refusal path for forecast-accuracy or skill-scoring requests, stating it is unavailable and not substituting a historical comparison; verify a test asserts the refusal and that no comparison is returned.

## 9. Comparison service

- [x] 9.1 Implement the criterion scoring functions — warmest, coolest, driest, wettest, least windy, and the composite outdoor-suitability criterion with disclosed weights and reported component contributions — rejecting unsupported criteria with the supported list; verify unit tests cover each criterion's ordering, the disclosed weighting, the reported contributions, and the unsupported-criterion error.
- [x] 9.2 Implement multi-location comparison with shared window/units/provider basis, per-candidate local-time windows, and the two-to-N bounds; verify unit tests cover a three-location ranking, a single-location rejection, an over-limit rejection, and candidates in differing timezones.
- [x] 9.3 Implement day-level comparison for a single location; verify a unit test asserts the driest day ranks first with its supporting analytics.
- [x] 9.4 Implement historical comparison mode with data-class labelling and refusal of mixed-class comparisons; verify unit tests cover a historical comparison and a mixed-class rejection.
- [x] 9.5 Implement tie handling within tolerance and partial-failure handling via concurrent retrieval converting failures into excluded candidates, raising below two survivors; verify unit tests cover a tie sharing a rank, one unavailable candidate among three, and only one candidate surviving.

## 10. MCP weather server

- [x] 10.1 Implement `mcp/server.py` with the MCP SDK, the configurable transport of design.md decision 13, and startup registration of the tool catalog; verify a test lists the server's tools over the in-memory transport and asserts all seven are present with schemas and descriptions.
- [x] 10.2 Implement `geocode_location`, `weather_current`, and `weather_forecast` with hand-written input schemas, declared output shapes, and results carrying location, period, units, provider, retrieval time, and data class; verify tests call each over the in-memory transport with fake providers and assert the attributed result shape.
- [x] 10.3 Implement `weather_history` and `weather_compare` with the same contract, including historical mode and excluded candidates; verify tests cover a history call, a multi-location compare, and a compare with an excluded candidate.
- [x] 10.4 Implement `weather_statistics` and `weather_anomaly` delegating to the deterministic analytics engine and reporting method, parameters, and point count; verify tests assert identical results for identical arguments and that both succeed with no inference credential configured.
- [x] 10.5 Implement input validation across all seven tools rejecting invalid calls before any provider or computation work, and the tool-not-found error listing available names; verify tests cover a missing required argument with no upstream call made, a zero-day horizon, and an unknown tool name.
- [x] 10.6 Implement the structured tool-error semantics distinguishing invalid input, unresolvable location, no data, unavailability, timeout, and rate-limiting, with no credential or raw payload leakage; verify tests assert each class is distinguishable and that no error body contains credential material.
- [x] 10.7 Implement the MCP client used by the backend, including fail-fast startup reporting when the configured server is unreachable; verify a test asserts the startup failure message names the MCP server and its address.
- [x] 10.8 Add a boundary test asserting no MCP module imports the agent graph, the API routers, the auth layer, or the frontend, and that every tool is exercisable without constructing the graph or a principal; verify the test passes.

## 11. RAG subsystem

- [x] 11.1 Author the curated corpus under `rag/corpus/` covering every required topic in `specs/rag-knowledge`, each document carrying identifier, title, topic, and provenance front-matter; verify a test asserts every required topic is present, every document has complete front-matter, and no document contains a location-specific measurement.
- [x] 11.2 Implement `rag/embed.py` with the `EmbeddingProvider` abstraction and the fastembed BGE-small implementation, recording model identity and dimension; verify a test asserts a stable vector dimension and deterministic output for identical input.
- [x] 11.3 Implement `rag/store.py` over pgvector with the recorded embedding model identity and dimension, refusing queries on mismatch; verify `db` tests cover a successful store-and-query cycle and a mismatched-model query failing with a re-index-required error.
- [x] 11.4 Implement `rag/ingest.py` with heading-aware token-bounded chunking, idempotent re-ingestion, replacement of changed documents, and reported document and chunk counts, run under the privileged connection; verify `db` tests cover a first ingest, an idempotent re-ingest, and a changed document being replaced rather than duplicated.
- [x] 11.5 Implement `rag/retrieve.py` with top-k bounding, a relevance threshold, and per-chunk text, document identifier, title, and score; verify `db` tests cover relevant retrieval, a bounded result count, and a below-threshold query returning empty rather than the least-bad match.
- [x] 11.6 Add tests asserting retrieval functions with no inference credential configured and that the corpus is readable by any authenticated user without being user-owned; verify both pass.

## 12. Memory layer (user- and thread-scoped)

- [x] 12.1 Implement `memory/checkpointer.py` wiring the LangGraph Postgres checkpointer with the composed `{user_id}:{thread_id}` key of design.md decision 11; verify a `db` test asserts graph state persists across a re-instantiated checkpointer and that a key composed for one user cannot address another user's checkpoint.
- [x] 12.2 Implement `memory/threads.py` — thread creation recording its owning user, the resolved-entity projection holding locations, units, window, criterion, and last data class, and the ownership check applied before the graph is invoked; verify `db` tests cover recording resolved entities, resolution surviving a restart, an unresolved reference reported rather than guessed, two threads of one user staying separate, and a foreign thread id failing without disclosing existence.
- [x] 12.3 Implement `memory/preferences.py` — preference read, update, and delete scoped to the authenticated user with documented defaults reported as defaults, and no inferred persistence; verify `db` tests cover setting and applying a preference, defaults when unset, deletion restoring defaults, that repeated use of a location does not persist it, and that one user's preference does not affect another's.
- [x] 12.4 Implement saved locations scoped to the authenticated user — add, list, remove, canonical storage, duplicate suppression, and the enforced limit; verify `db` tests cover each, including a duplicate save not duplicating, the limit error stating the limit, and two users' lists staying separate.
- [x] 12.5 Implement `memory/retention.py` — bounded thread and checkpoint retention with an invocable retention routine run under the privileged connection, explicit per-thread deletion, and whole-account data deletion; verify `db` tests cover expired thread removal, confirmed on-request thread deletion, account deletion removing only the requesting user's records, and shared data surviving an account deletion.
- [x] 12.6 Implement honest degradation when the memory store is unavailable — public capabilities continue, follow-ups report context unavailable; verify tests cover a successful public forecast during a memory outage and a follow-up reporting unavailable context rather than answering without it.

## 13. LLM abstraction

- [x] 13.1 Implement `agents/llm/base.py` with the `LLMClient` Protocol and Weathra-owned message and completion types per design.md decision 3; verify a test asserts a fake client satisfies the Protocol.
- [x] 13.2 Implement `agents/llm/fake.py` — a scripted fake supporting both `complete` and `complete_json`; verify a test drives a scripted routing decision and a scripted prose completion through it.
- [x] 13.3 Implement `agents/llm/openrouter.py` over the shared HTTP client with configurable provider, model, timeout, and retries, reporting provider and model identity, and translating transport and status failures into Weathra errors; verify respx tests cover a prose completion, a JSON completion, a timeout, a 429, and a 401 surfacing as a configuration error.
- [x] 13.4 Implement `complete_json` schema enforcement — parse, validate against the pydantic schema, and retry a bounded number of times with the validation error appended; verify unit tests cover valid JSON first time, malformed JSON corrected on retry, and repeated failure raising.
- [x] 13.5 Implement lazy client construction raising `AgentNotConfigured` when no credential resolves, wired so only the agent routes construct it; verify a test asserts every public weather, history, analysis, and comparison endpoint plus RAG retrieval succeeds with no credential while agent construction raises.
- [x] 13.6 Add a test asserting no backend file imports a model-vendor SDK or hard-codes a model identifier; verify the test passes and fails when a hard-coded model name is introduced.

## 14. LangGraph orchestration

- [x] 14.1 Implement the graph state schema carrying the question, the acting principal's user identity, the thread key, the routing plan, per-capability structured results, the evidence record, and the response envelope; verify a unit test constructs and serializes the state and asserts the user identity is present and not caller-supplied.
- [x] 14.2 Implement `agents/supervisor.py` — the routing node requesting a schema-validated JSON plan, with the bounded retry and the deterministic keyword-and-entity fallback router of design.md decision 2, recording selected capabilities and the reason; verify fake-LLM tests cover a valid plan, a malformed plan corrected on retry, and repeated failure falling back to the deterministic router.
- [x] 14.3 Implement the forecast and historical capability nodes executing through the MCP client and recording their results and attribution into the evidence record; verify fake-LLM tests assert a forecast question routes to and executes the forecast node, and a historical question the historical node.
- [x] 14.4 Implement the analytics capability node executing deterministic analytics over a prior node's retrieved series; verify a fake-LLM test asserts a mean-temperature question retrieves then computes, with both nodes recorded in order.
- [x] 14.5 Implement the RAG capability node executing retrieval and recording cited chunk identifiers; verify a fake-LLM test asserts a conceptual question routes to the RAG node and calls no weather tool.
- [x] 14.6 Implement multi-step planning and execution across several nodes, sequential and parallel, passing structured results forward and addressing every part of a multi-part question; verify fake-LLM tests cover a forecast-plus-historical-plus-analytics question, a mixed conceptual-and-numerical question with per-part labelling, and a three-part question where an unanswerable part is named.
- [x] 14.7 Implement `agents/nodes/synthesize.py` — prose-only synthesis given structured results, with the system prompt of design.md decision 15 forbidding computation and figure introduction; verify a fake-LLM test asserts the prose is placed in the envelope's interpretation field and that no envelope figure originates from the model.
- [x] 14.8 Implement `agents/evidence.py` assembling the full evidence record — agents, tool calls and results, analytics results with methods, cited chunks, data classes, provider and model, and per-step timings — and persisting it to `agent_runs` owned by the acting user; verify `db` tests assert the record's completeness, that a figure in the answer is locatable within it, and that the run is owned by the acting user.
- [x] 14.9 Implement bounded execution on graph steps and wall-clock duration, returning a partial result reporting what was gathered; verify fake-LLM tests cover completion within budget, step-budget exhaustion, time-budget exhaustion, and evidence retained for a partial answer.
- [x] 14.10 Implement `agents/grounding.py` — the hard zero-retrieval guard, the numeric audit reporting ungrounded figures without suppressing the answer, and the grounding report in the envelope; verify fake-LLM tests cover prose with figures and no retrieval being discarded, an ungrounded figure reported with `verified: false`, and a fully grounded answer verifying clean.
- [x] 14.11 Implement per-user thread context use for follow-up resolution, stating the resolved location and window, and asking rather than assuming when a reference is unresolvable; verify `db` plus fake-LLM tests cover the Berlin/Munich follow-up, unit preference carried forward, an unresolvable reference producing a question, and a second user's follow-up resolving only against their own context.
- [x] 14.12 Implement preference-derived defaults and ambiguity surfacing — the acting user's saved default location used and disclosed, ambiguous location presented as candidates, no location anywhere producing a clarifying question; verify tests cover all three.
- [x] 14.13 Implement scope confinement for non-weather questions and the untrusted-content rule for provider text, place names, tool results, and knowledge chunks; verify fake-LLM tests cover an out-of-scope question declined, an instruction embedded in a tool result ignored, and an instruction embedded in a knowledge chunk ignored.
- [x] 14.14 Implement the tool-catalog constraints — only approved weather, analytics, and knowledge capabilities offered; an out-of-catalog capability in a plan returning an error to the model; invalid arguments returned for correction within the remaining budget; verify fake-LLM tests cover each and assert no capability permits writes, code execution, filesystem access, or arbitrary network requests.
- [x] 14.15 Implement the refusal paths — unresolvable location, period beyond coverage, provider failure mid-run, and a statistic no tool could compute — each naming the reason and reporting no substituted figures; verify fake-LLM tests cover all four.

## 15. HTTP API — REST

- [x] 15.1 Implement `api/app.py` with the versioned prefix, lifespan-managed HTTP client, database engines and MCP client, explicit non-wildcard CORS origins permitting the `Authorization` header, and OpenAPI with security metadata plus interactive documentation; verify tests assert the schema lists every endpoint marked public or protected and that the app starts and shuts down cleanly.
- [x] 15.2 Implement `api/errors.py` — the single error envelope, the status and code mapping of design.md decision 16 including 401 and 403, FastAPI's 422 overridden into that envelope at 400, and a 500 handler logging internals and returning none of them; verify tests assert the envelope shape and that validation, unauthenticated, forbidden, not-found, upstream-failure, timeout, rate-limit, dependency-unavailable, and internal errors each map to their distinct status with no token material in any body.
- [x] 15.3 Implement `api/middleware.py` — request-id correlation accepting a client-supplied id, contextvar binding, response and error-body propagation, and structured request logging of endpoint, outcome, duration, provider, cache status, acting user identity, and agent activity, with no credentials or tokens; verify tests assert the response header, the same id in an error body and in log records, and that no log record contains token material.
- [x] 15.4 Implement the public/protected route wiring using the principal dependencies, with the classification asserted in one place; verify a test enumerates every route and asserts its classification matches the classification in `specs/authentication`, and that every protected route returns 401 without a token.
- [x] 15.5 Implement the location endpoints as public — search with a limit, and resolution by name or coordinates with ambiguity as a marked success and unresolvable as a not-found error; verify API tests cover search, unique resolution, ambiguous resolution distinguishable from a single one, an unresolvable name, and success without a token.
- [x] 15.6 Implement the public current-conditions and forecast endpoints carrying location, provider, units, retrieval time, cache status, data class, and uncertainty, applying the caller's preferred units when they are authenticated; verify API tests cover retrieval by name, by coordinates, an unknown provider listing registered names, a zero-day horizon rejected with no upstream call, success without a token, and preferences applied with one.
- [x] 15.7 Implement the public history endpoint including period comparison and baseline computation; verify API tests cover observation retrieval labelled historical, a period comparison, a baseline reporting years used, and a range before archive coverage rejected.
- [x] 15.8 Implement the public analysis endpoint with optional window, units, thresholds, and statistic set, reporting method and point count per figure; verify API tests cover a request with thresholds, one without, and a non-computable statistic reported unavailable while others return.
- [x] 15.9 Implement the public comparison endpoint covering multi-location, day-level, and historical modes with excluded candidates and reasons; verify API tests cover a three-location comparison, a historical comparison, one with an excluded candidate, and a single-location validation rejection.
- [x] 15.10 Implement the protected `/ask` endpoint returning the answer envelope, evidence, data-class labels, and provider and model, accepting a thread identifier only when the acting user owns it, and returning 503 naming the missing configuration when no inference credential is set; verify API tests with an injected fake LLM cover a successful answer, a follow-up in an owned thread stating what it resolved to, a foreign thread refused without disclosure, an unauthenticated request returning 401, and the unconfigured case returning 503 while a public endpoint still succeeds.
- [x] 15.11 Implement the protected current-user endpoint returning the acting user's subject, email, profile creation time, and effective preferences, creating the profile on first use and returning no credential material; verify API tests cover a first call creating the profile, a subsequent call, and an unauthenticated call returning 401.
- [x] 15.12 Implement the protected preferences and saved-locations endpoints scoped to the acting user; verify API tests cover reading and updating preferences, adding, listing, and removing a saved location, unauthenticated access returning 401, isolation between two users, and a cross-user mutation refused.
- [x] 15.13 Implement the protected evidence endpoint returning one of the acting user's stored records, with the same not-found error for an unknown identifier and for another user's; verify API tests cover retrieval, an unknown identifier, and a foreign identifier returning an identical response to the unknown one.
- [x] 15.14 Implement the protected account data-deletion endpoint removing the acting user's saved locations, preferences, threads, memory, and agent runs and confirming what was removed; verify API tests cover a deletion, that another user's records survive it, and that shared data survives it.
- [x] 15.15 Implement the public health endpoint and readiness report naming the weather provider, database, vector store, MCP server, authentication provider, and inference provider with configured and reachable status, disclosing no credentials and reporting healthy when only inference is unconfigured; verify API tests cover both endpoints in the unconfigured case and assert no key material appears.

## 16. HTTP API — SSE streaming

- [x] 16.1 Implement the protected SSE stream endpoint mapping LangGraph events into Weathra's typed events (`routing`, `agent_start`, `agent_end`, `tool_start`, `tool_end`, `answer_delta`, `final`, `error`), each carrying the request id and a monotonic sequence number; verify tests assert event ordering, that every event carries correlation and ordering fields, and that the stream ends with an explicit completion event.
- [x] 16.2 Implement stream authentication — token validated before any run begins, the principal held for the stream's lifetime, and a terminal authentication error event when the session becomes invalid mid-stream; verify tests cover an authenticated stream applying the user's memory and preferences, an unauthenticated request returning 401 with no run started, and a mid-stream invalidation emitting the terminal event and closing.
- [x] 16.3 Implement terminal error emission and client-disconnect handling that abandons the run without an unhandled server error; verify tests cover a mid-run failure emitting a terminal error and closing, and a client disconnect producing no unhandled error.
- [x] 16.4 Verify SSE works cross-origin with the `Authorization` header from a configured origin, and that the platform request timeout exceeds the agent wall-clock budget which in turn sits below the token lifetime; verify an API test streams successfully from an allowed origin with a token and a documented configuration check records both timing relationships.

## 17. Safety and grounding verification

- [x] 17.1 Implement data-class labelling end to end so every reported value in every response carries its class and mixed-class answers label each part; verify tests across the forecast, history, analysis, comparison, and ask endpoints assert labels are present and never conflated.
- [x] 17.2 Implement structured attribution — provider, location, period, and retrieval time in response fields rather than prose — on every weather-bearing response, with per-source attribution for multi-source answers; verify tests assert machine-readable attribution on each endpoint and separate attribution for a forecast-plus-historical answer.
- [x] 17.3 Implement the forecaster-positioning and official-warnings requirements — forecasts attributed to the provider, severe-weather questions referred to the official local authority, and no emergency-warning framing; verify fake-LLM tests cover a severe-weather question producing a referral and a question about whether Weathra forecasts producing the correct description.
- [x] 17.4 Implement the unsupported-severe-weather-claim guard — no assertion of presence, severity, or likelihood without a supporting retrieved field; verify tests cover a severity claim attempted without supporting fields being blocked and a supported claim reported with attribution.
- [x] 17.5 Implement honest unavailability across every path — provider failure, period outside coverage, absent measure, and partial availability each stated precisely with no substitution; verify tests cover all four.
- [x] 17.6 Add data-minimization tests asserting persisted data contains only the declared categories, that application tables hold no credential or duplicated contact data, that logs contain no credentials or tokens, and that thread content is gone after retention elapses; verify all four pass.

## 18. Authentication and authorization test suite

Consolidated cross-cutting verification of the identity boundary. Individual groups above test their own slices; this group proves the boundary as a whole.

- [x] 18.1 Implement the test fixtures — a local signing key pair seeded into the JWKS cache, a token factory producing valid, expired, wrong-issuer, wrong-audience, bad-signature, unknown-key-id, and malformed tokens, and two distinct authenticated test principals; verify the fixtures produce tokens the validator accepts and rejects as intended with no network access.
- [x] 18.2 Verify the valid-request case across every protected endpoint — each returns success and acts as the token's subject; verify the parameterized test covers every protected route.
- [x] 18.3 Verify the missing-token case across every protected endpoint — each returns 401 and performs no read or write; verify the parameterized test covers every protected route and asserts no database mutation occurred.
- [x] 18.4 Verify the expired-and-invalid-token cases across every protected endpoint — expired, bad signature, wrong issuer, wrong audience, unknown key id, and malformed each return 401 with distinguishable codes and no token material in the body; verify the parameterized test covers every case.
- [x] 18.5 Verify user A cannot read user B's data — saved locations, preferences, threads, agent runs, and evidence — by identifier, by listing, or by any other route; verify each attempt returns the same response as for a non-existent record.
- [x] 18.6 Verify user A cannot mutate user B's data — update, delete, and thread-append attempts across every user-owned resource; verify each fails and user B's records are byte-for-byte unchanged afterwards.
- [x] 18.7 Verify saved-locations, preferences, and persistent-memory isolation as three explicit named tests matching `specs/authentication`'s scenarios; verify all three pass.
- [x] 18.8 Verify the authenticated SSE case — a valid token streams and applies the user's memory and preferences, a missing token returns 401 with no run started, and a foreign thread is refused; verify all three.
- [x] 18.9 Verify the RLS gate independently of the data path — a query with its ownership predicate deliberately omitted, run under one user's claims against another's row, returns nothing; verify the `db` test passes and fails if the policy is dropped.
- [x] 18.10 Verify frontend protected-route behavior — an unauthenticated visit to a protected route redirects to sign-in with the destination preserved, an authenticated visit renders, no protected content flashes before the session resolves, and a 401 from the API produces the expired-session state rather than a data error; verify the component and Playwright tests cover each.
- [x] 18.11 Verify secret containment — the service-role key and database URLs are absent from the frontend environment and the built bundle, and no secret carries a `NEXT_PUBLIC_` prefix; verify the CI check fails when a secret is deliberately introduced into the frontend environment.

## 19. Visily design phase

Completes before substantial frontend implementation in groups 20 and 21. Visily.ai is the design tool for the remaining screen design and design-system refinement; the earlier UXPilot exploration is preserved as prior design exploration and its approved decisions are carried in as inputs, not rediscovered. No paid Visily export capability and no second design tool — Figma included — is required: an approved screen is a visual reference, implemented by hand in Next.js in groups 20 and 21.

- [x] 19.1 Produce the shared Weathra design system in Visily — typography, spacing scale, component hierarchy, navigation, cards, charts, weather visualization patterns, responsive behavior, and the loading, empty, error, and authentication state patterns — seeded from the design direction carried forward from the earlier UXPilot exploration per `specs/web-ui`: the Midnight Intelligence palette, Plus Jakarta Sans for display and heading type with Inter for body and UI, the Intelligent Command Center shell with persistent left navigation, and the premium modern SaaS level of finish; record it under `docs/design/` with an index, noting the UXPilot origin of each carried-forward decision; verify the recorded system covers every item listed in `specs/web-ui`, carries every carried-forward decision with any departure recorded with its reason, and is referenced by the component tasks that follow.
- [x] 19.2 Produce the authentication screen designs in Visily — Sign In, Create Account, Verify Email / Enter Verification Code, Verification Successful, Verification Failed / Expired Code, Resend Verification Code, Forgot Password, Reset Password — including every state each screen can be in; verify each has a recorded artifact covering its success, failure, expired, and in-progress states and following the recorded design system.
- [x] 19.3 Produce the core product screen designs in Visily — the location-focused Dashboard carrying the Weathra Intelligence briefing and What Changed?, AI Weather Analyst with Why?, Historical Analytics, Compare Cities, Agent Evidence / Activity, Saved Locations, Settings — including loading, empty, and error states, the shared Intelligent Command Center shell, and the presentation of data class (observed, forecast, historical, deterministic analytics, AI interpretation), attribution, timestamps, and uncertainty; verify each has a recorded artifact and that no screen's design implies the language model produced a numerical weather value.
- [x] 19.4 Record the post-MVP screens in the design roadmap — Weather Intelligence Report, Forecast Explorer, Weather Scenario Lab, Weather Watch, Travel Intelligence — as design-roadmap entries rather than MVP artifacts; verify `docs/design/` and `docs/roadmap.md` agree on the classification.
- [x] 19.5 Review and approve the Visily artifacts as the implementation reference, and record the approval; verify every MVP screen has an approved artifact recorded before its implementation task is started, and that the gate was satisfied with no paid export capability and no second design tool.

## 20. Frontend foundation and authentication

- [x] 20.1 Implement the Supabase client layer — browser client, server client, and middleware helper using the SSR/cookie session approach of design.md decision 18, reading only public configuration; verify tests assert cookie-based session handling and that no server-side secret is referenced.
- [x] 20.2 Implement `middleware.ts` — session refresh and the protected-route gate over the `(app)/` group, redirecting unauthenticated requests to sign-in with the destination preserved; verify tests cover an unauthenticated redirect preserving the destination, an authenticated pass-through, and a refreshed session.
- [x] 20.3 Implement the design-system foundation in code from the approved group 19 Visily artifacts, by hand rather than from any export — tokens for typography, spacing, and color carrying the Midnight Intelligence palette and the Plus Jakarta Sans and Inter type roles, with a `prefers-color-scheme` override, plus the primitive components; verify component tests assert the tokens match the recorded design system and contrast meets 4.5:1 in both appearances.
- [x] 20.4 Implement the Create Account screen with the password rules stated before submission and a non-disclosing already-registered response; verify component tests cover a successful submission, a password failing the rules naming it, and the already-registered case not confirming existence.
- [x] 20.5 Implement the Verify Email screen with code entry, the success state, the incorrect-code and expired-code states, and the resend action with in-progress, confirmed, and rate-limited states; verify component tests cover each state.
- [x] 20.6 Implement the returning-link route handler completing verification without further entry and showing the success state, and the equivalent path for password recovery; verify tests cover a valid link, an expired link, and an already-used link.
- [x] 20.7 Implement the Sign In screen with a non-disclosing failure state and routing of an unverified account to the verification step; verify component tests cover success, an identical message for unknown email and wrong password, and the unverified routing.
- [x] 20.8 Implement Forgot Password and Reset Password with an identical response for known and unknown addresses, the password rules stated, and an expired-reset state; verify component tests cover the full reset path, the unknown-address case, an expired reset, and a new password failing the rules.
- [x] 20.9 Implement sign-out and the session-state layer — persistence across reload, transparent refresh, the pending state that does not flash protected content, and the shared 401 interceptor producing the expired-session state and returning the person to sign-in; verify component tests cover sign-out clearing state, persistence across reload, the pending state, and a 401 producing the expired-session path rather than a data error.
- [x] 20.10 Implement the redirect of an already-authenticated person away from the authentication screens; verify a test asserts it.
- [x] 20.11 Implement the typed API client attaching the access token as a bearer header to every protected call and the response types matching the backend's OpenAPI models; verify tests assert the header is attached, refreshed tokens are used, and response types match fixtures captured from the backend schema.
- [x] 20.12 Implement the app shell — protected layout resolving the session server-side, persistent navigation identifying who is signed in, sign-out control, and post-MVP route stubs stating they are not yet available; verify tests assert every MVP product screen is reachable, the signed-in identity is shown, and a post-MVP route renders its not-yet-available state.
- [x] 20.13 Implement the shared query layer and the `loading | empty | error | ready` convention with retry that needs no page reload; verify component tests cover each state and a retry after a failed request.
- [x] 20.14 Implement the `useAgentStream` hook consuming the SSE endpoint with the bearer token via a streamed fetch body reader, exposing ordered events and a terminal state including the authentication-error terminal event; verify tests cover ordered event delivery, a terminal error, an interrupted stream surfacing what was received, and a mid-stream authentication error routing to the expired-session state.
- [x] 20.15 Implement the shared presentation primitives from the design system — data-class badges, attribution footers, uncertainty indicators, and the visual distinction for AI interpretation; verify component tests assert each renders its label and that interpretation is distinguishable from retrieved data.

## 21. Frontend MVP product screens

- [x] 21.1 Implement the Dashboard — the Weathra Intelligence briefing with current conditions, forecast movement, anomalies, historical context, and What Changed?, each data-class labelled and attributed, using the signed-in person's default location and units; verify component tests cover a populated briefing, a location with no prior snapshot, a person with no saved default, and the loading and error states.
- [x] 21.2 Implement the AI Weather Analyst — question entry, streamed routing/agent/tool progress, the labelled answer with attribution and uncertainty, submit disabled while pending, thread continuity for follow-ups, and the agent-unavailable message naming the missing configuration; verify component tests cover a streamed answer, a follow-up resolving from thread context, a duplicate submission prevented, an interrupted stream, and the unavailable case.
- [x] 21.3 Implement Historical Analytics — historical retrieval, period comparison, and baseline comparison with charts and historical labelling; verify component tests cover a period comparison, a baseline comparison stating years used, and a range rejected as outside coverage.
- [x] 21.4 Implement Compare Cities — multiple location entry, criterion selection, ranked results with supporting values, excluded candidates with reasons, and a client-side block below two locations; verify component tests cover a three-city ranking, an excluded candidate, and a single-location attempt blocked.
- [x] 21.5 Implement Agent Evidence / Activity — the run record showing agents, tool calls and results, analytics methods, cited knowledge, and timings, reachable from an answer and limited to the signed-in person's own runs; verify component tests cover a populated record, an unknown run identifier, and another user's identifier producing the same not-found treatment.
- [x] 21.6 Implement Saved Locations and Settings — list, add, and remove saved locations; unit system, default horizon, default location, sign-out, session-memory deletion, and account data deletion with confirmation; verify component tests cover saving and removing a location, changing units and seeing later screens reflect it, deleting thread memory with confirmation, and account data deletion with an explicit confirmation step.
- [x] 21.7 Implement ambiguous-location handling across every location-entry surface, presenting candidates and showing data only after a choice; verify component tests cover an ambiguous entry on the Dashboard and on Compare Cities.
- [ ] 21.8 Verify accessibility and responsiveness across all MVP screens, authentication and product alike — keyboard-only operation of every action, labelled inputs, accessible control names, 4.5:1 body-text contrast in both appearances, usability at a 360-pixel viewport with no horizontal page scroll, and wide content scrolling in its own container; verify with automated accessibility assertions plus a recorded manual pass.
- [x] 21.9 Verify every implemented screen against its approved Visily artifact and record any deliberate divergence with its reason; verify the review covers all fifteen MVP screens and is recorded in `docs/design/`.
- [x] 21.10 Implement the Playwright flows — sign up through verification into the product; ask a question then open its evidence; save a location then see the unit preference applied; and an expired session routed to sign-in; verify all four pass against a backend running with a fake inference provider.
- [x] 21.11 Audit all eight approved screens against the running application in the states people actually meet — populated, empty, loading, error and unauthenticated — classifying every difference, correcting the mechanical ones and redesigning the surfaces whose character had drifted without losing grounding, provenance, uncertainty or evidence; verify the audit, its fixes and what it deliberately left are recorded in `docs/design/runtime-fidelity-audit.md`.

## 22. Evaluation subsystem

- [x] 22.1 Implement the dataset format and author approximately 40 cases as structured data files under `evaluation/dataset/`, with at least four cases in each of the six required categories, each declaring identifier, category, question or turn sequence, expected tools or agents, forbidden tools, expected answer characteristics, reference values, and per-turn expected resolution; verify a test asserts the composition, that every case declares every required field, and that the dataset is data rather than test literals.
- [x] 22.2 Implement repeatable evaluation test-user provisioning under the privileged connection, obtaining a real token for that user, and recording the identity without its credential; verify a test asserts a run starting with no existing test user provisions one and authenticates without manual intervention.
- [x] 22.3 Implement `evaluation/metrics.py` computing all ten metrics exactly as defined in `specs/evaluation`, each reporting numerator, denominator, and failing case identifiers, with inapplicable cases excluded from denominators; verify unit tests over synthetic run records cover each metric including an inapplicable-case exclusion and latency reported as median and 95th percentile.
- [x] 22.4 Implement threshold evaluation reporting per-threshold and overall pass or fail, naming missed thresholds and their margins, and reporting non-gating metrics regardless; verify unit tests cover an all-pass run, a run failing numerical accuracy, and non-gating metrics present in both.
- [x] 22.5 Implement `evaluation/runner.py` executing the dataset as the authenticated test user with category and case filters, invocable from the command line and CI, recording per-case results with evidence and the full run configuration; verify a test runs a filtered subset in offline mode and asserts the run record's configuration fields, the test-user identity without its credential, and retained evidence for a failing case.
- [x] 22.6 Implement offline mode over recorded weather fixtures and the fake inference provider, with reference values computed over those same fixtures and locally-minted tokens, and mode recorded in the run; verify the deterministic metrics compute with no external network access and the run record states offline mode.
- [x] 22.7 Implement run persistence and cross-run comparison reporting per-metric change and flagging a dataset-version difference; verify `db` tests cover comparing two runs and comparing across dataset versions.
- [x] 22.8 Record every language model call attempt in the evidence record — the call stage, the attempt number, the outcome (served, invalid output, rate limited, model unavailable, provider error, timeout or network failure, not configured), the provider, the selected model, the model the gateway reported as having served the call, the provider status where the failure had one, and the reason the run continued as it did — leaving the deterministic router and the code-written summary exactly as they behave today; verify unit tests cover a served call recording the model that answered, a 404, a 429, a 5xx, a timeout and a network failure each classified distinctly, a schema-validation failure recorded as a served call with invalid output rather than as a provider failure, a run with no credential recording that on every stage, and a fully fallen-back run reporting that no model materially served it while still returning its figures, attribution and grounding.
- [x] 22.9 Implement evaluation inference integrity — per-case served-model determination from the recorded attempts, quarantine of unserved cases from every metric numerator and denominator counting them neither as passes nor as failures, run classification as a provider failure below the configured served-case floor or when quarantine empties a gated threshold, no threshold verdict and no model-quality claim for such a run, retained per-case evidence, persistence with no pass-or-fail verdict, a distinct non-success exit status, a live pre-flight probe aborting before any case executes, configurable inter-case pacing, and bounded rate-limit retry honouring a stated delay up to a configured maximum wait; verify tests cover a fully fallen-back run classified as a provider failure rather than a threshold failure, a rate-limited run likewise, a single quarantined case excluded from both halves of every metric, invalid output still scored, a provider-failure run persisting with no verdict and comparing as not scored, the pre-flight aborting after exactly one gateway call, and the offline run still passing every threshold and classified not applicable.
- [x] 22.10 Execute a live evaluation run against the configured OpenRouter model, record its results, and report which thresholds passed and which did not; verify the run record exists with its provider and model and the outcome is recorded in `docs/evaluation.md` — reporting a failing threshold honestly rather than adjusting the threshold.

## 23. CI/CD, secrets, and deployment

- [x] 23.1 Implement the pull-request CI workflow — backend lint, type-check, and tests with a Postgres+pgvector service; frontend lint, type-check, tests, and build; the secret-exposure check; and the offline evaluation run; verify the workflow passes on a pull request with no local-machine step.
- [x] 23.2 Configure the Supabase project and record its required configuration as version-controlled notes — email confirmation required, confirmation and recovery templates carrying the one-time token, per-environment redirect URLs, and token lifetimes below which the agent budget sits; verify a real sign-up delivers a usable code and the documented configuration matches the project.
- [x] 23.3 Configure secret storage for CI and the deployed Render backend, holding the service-role key, database URLs, and inference key server-side only; verify no secret is present in the frontend environment or repository and the deployed backend starts with the required secrets injected.
- [x] 23.4 Containerize the backend and implement the Render deployment workflow with Alembic migrations, including the RLS policies, applied under the privileged connection before the new backend release serves traffic, on an instance type that does not idle-spin-down; verify a deployment succeeds and readiness reports every dependency reachable.
- [x] 23.5 Implement the Vercel deployment workflow for the frontend with the public Supabase configuration and backend base URL set per environment; verify the deployed frontend loads, signs a user in, and reaches the deployed backend with a bearer token.
- [x] 23.6 Configure the Supabase database — pgvector enabled, pooler connection, RLS policies applied, and the retention routine invoked on a schedule from CI under the privileged connection; verify migrations apply, the `db` suite passes against it, and a scheduled retention invocation completes.
- [x] 23.7 Verify the no-local-machine requirement end to end by completing setup, sign-up, test, and deploy from a browser-based cloud environment; verify each step and record the confirmation in `docs/deployment.md`.

## 24. Documentation

- [x] 24.1 Write the README — what Weathra is, the architecture at a glance, the MVP versus post-MVP split, and how to get from clone to a running pair of applications with a verified account; verify a reader following it reaches a working cloud setup and can sign in.
- [x] 24.2 Write `docs/architecture.md` and `docs/agents.md` covering the component boundaries, the dependency rule, the graph topology, the four agents' responsibilities, and the graph-controlled-execution decision; verify each documented component matches the implemented module layout.
- [x] 24.3 Write `docs/authentication.md` — the Supabase Auth flows, the verification and reset paths including the code and link variants, backend token validation and key caching, the principal, the ownership model and table classification, the RLS policies, the two database roles, and the secret split; verify every requirement in `specs/authentication` is addressed and the documented table classification matches the migrations.
- [x] 24.4 Write `docs/configuration.md` and `docs/api.md` — every environment variable for both applications with defaults and its public/secret classification, and the API surface with the OpenAPI reference, the public/protected classification per endpoint, and the SSE event catalogue; verify every variable in `Settings` and every route appears with its classification.
- [x] 24.5 Write `docs/mcp.md` and `docs/rag.md` — the MCP tool catalogue with schemas and error semantics, and the corpus, ingestion, embedding, and retrieval design including the re-index requirement; verify each documented tool and topic matches the implementation.
- [x] 24.6 Write `docs/evaluation.md` — the dataset composition, all ten metric definitions, the thresholds, how to run the suite including test-user provisioning, and the latest recorded results; verify the definitions match `evaluation/metrics.py` exactly.
- [x] 24.7 Write `docs/privacy-ethics.md` — the data-class and attribution rules, the uncertainty and severe-weather stance, the grounding layers and their honest limits, what is persisted and for how long, where credential and contact data live, and how a person deletes their data; verify every requirement in `specs/safety-grounding` is addressed.
- [x] 24.8 Write `docs/deployment.md` and `docs/roadmap.md` — the deployment topology, environments, required Supabase configuration, rollback, and the MVP versus post-MVP roadmap matching Part B below; verify the roadmap and Part B agree.
- [x] 24.9 Record the approved Visily artifacts and design system under `docs/design/` with an index mapping each MVP screen to its artifact and each post-MVP screen to its roadmap entry, and record the carried-forward design direction with its UXPilot origin as prior design exploration; verify every MVP screen has an entry and every carried-forward decision is accounted for.

## 25. Acceptance verification

- [x] 25.1 Run the full backend verification pass — ruff, type-check, `pytest`, and `pytest -m db` — and record any failure rather than working around it; verify all commands pass.
- [x] 25.2 Run the full frontend verification pass — lint, type-check, unit tests, build, and the Playwright flows; verify all pass.
- [ ] 25.3 Run the authentication and authorization suite of group 18 against the deployed backend and record the results; verify every case passes, including cross-user isolation and the RLS gate.
- [ ] 25.4 Run the live smoke check against the deployed pair — create an account and verify it by code, sign in, readiness all-reachable, an attributed public forecast without a session, a baseline comparison with both sides labelled, a question through `/ask` whose every figure appears in its evidence, an authenticated SSE stream completing, and a second account seeing none of the first's data; verify each and record the results.
- [x] 25.5 Trace every capability spec to its implementing tasks and tests, and record the traceability table in `docs/architecture.md`; verify every requirement in all twenty specs maps to at least one test.

---

## 26. SaaS-ready schema, migrations, and Row Level Security

- [x] 26.1 Add the domain types the SaaS layer needs to `domain/` — `CallRole` (routing, synthesis, lab), plan code, policy identifier, quota dimension and window key, `Resolution`, and the usage-event record — and extend the error hierarchy with `NoEligibleModel`, `ModelNotAllowlisted`, `PolicyUnavailable`, and `QuotaExceeded`; verify a test asserts each type is frozen, each error maps to exactly one HTTP status through the existing single mapping point, and `domain/` still imports nothing internal.
- [x] 26.2 Write the Alembic migration creating the operational tables — `subscription_plans` (plan code, display name, per-role policy mapping, allowances reference, null external subscription reference), `model_policies` (policy id, ordered candidate catalog keys, applicable call roles, eligibility, declared fallback policy), `model_catalog` (catalog key, gateway provider and model id, display name, capability roles, tier, structured-output support, context window, input and output price, currency, pricing date, status, free-or-paid), and `usage_limits` (plan or internal subject, dimension, window, allowance) — with the uniqueness constraints of `specs/model-catalog`; verify a `db` test asserts a duplicate catalog key, a duplicate gateway provider-and-model pair, a negative price, and a non-positive context window are all rejected by the database.
- [x] 26.3 Write the Alembic migration creating the user-owned tables — `user_plans`, `usage_counters` keyed by subject, dimension and window key, and `llm_usage_events` with every field `specs/llm-telemetry` requires plus the generated internal classification of design.md decision 27 — with Row Level Security enabled and owner-restricting policies applied in the same migration; verify a `db` test asserts each table has RLS enabled with an owner policy and that a query omitting the owner predicate under the restricted role returns only the acting principal's rows.
- [x] 26.4 Write the Alembic migration creating the operational, non-user-owned tables — `model_evaluations`, `model_comparison_runs`, `model_comparison_results`, and `admin_audit`; verify a `db` test asserts the request-serving restricted role cannot read or write them on behalf of an ordinary user.
- [x] 26.5 Add a test asserting that the Row Level Security policies on `profiles`, `preferences`, `saved_locations`, `threads`, the LangGraph checkpoint tables, and `agent_runs` are byte-for-byte unchanged by this change and that no new migration drops, alters, or adds a bypass to any of them; verify the test passes and fails when a policy is weakened.
- [x] 26.6 Write the seed migration establishing the initial catalog entries as data, the four shipped policies (`free_default`, `balanced`, `high_reasoning`, `admin_experimental`) with their ordered candidate lists and declared fallbacks, the Free, Pro, and Premium plans with their per-role policy mappings, and the initial allowances including the internal allowance; verify a `db` test asserts a fresh migrated database has at least one enabled catalog entry for every capability role a shipped policy requires, and that no shipped policy references a missing catalog key.
- [x] 26.7 Add a test asserting every request-path read and write of `user_plans`, `usage_counters`, and `llm_usage_events` runs through the request-scoped restricted session rather than the privileged connection; verify the test passes and fails when a handler is given a privileged session.
- [x] 26.8 Extend the seed migration with the fixed-model evaluation policy — exactly one candidate, failover disabled, no declared fallback, eligible only to the internal evaluation subject — and with `free`, `pro` and `premium` as the canonical plan codes; verify a `db` test asserts the evaluation policy is unreachable by every product plan, that it declares exactly one candidate, that `free`, `pro` and `premium` each exist, and that no plan code named `plus` exists and none can be reintroduced.

## 27. Model catalog, plan, and policy stores

- [x] 27.1 Implement `entitlements/catalog.py` — the catalog repository with read, list filtered by status and capability role, and administrative create, edit, enable and disable, validating every rule of `specs/model-catalog` including the refusal to disable the last enabled entry for a required capability role without an explicit acknowledgement; verify `db` tests cover a successful write, each validation refusal, and the last-model-for-a-role refusal with and without the acknowledgement.
- [x] 27.2 Implement `entitlements/plans.py` and `entitlements/policies.py` — the plan, plan-mapping, allowance, and policy repositories with administrative writes, refusing a policy that references a catalog key that does not exist and an allowance with a negative value or unknown dimension; verify `db` tests cover a plan mapping change taking effect, a dangling policy reference refused at write time, and an invalid allowance refused.
- [x] 27.3 Implement the TTL snapshot cache of design.md decision 23 over the catalog, policy, and plan reads with `MODEL_CATALOG_CACHE_TTL_SECONDS`, refreshed on stale read, and bypassed by direct database validation for administrative overrides and lab selections; verify tests assert a change is visible after the TTL, that a hot-path resolution issues no query while the snapshot is fresh, and that an override validates against the database rather than the snapshot.
- [x] 27.4 Extend the group 13.6 no-hard-coded-model test into the vendor-identifier confinement test of design.md decision 23 — a gateway model identifier may appear only in `db/migrations/`, `.env.example`, and the OpenRouter adapter's outbound request body, and no policy record, plan mapping, quota rule, agent, node, route, evaluation case, or user-facing string may select behavior by a vendor model identifier or family name; verify the test passes and fails when a model name is introduced into application logic, a policy rule, or a UI string.

## 28. Model policy resolver

- [x] 28.1 Implement `entitlements/resolver.py` — the `ModelPolicyResolver` Protocol and the concrete resolver walking the five ordered steps of design.md decision 22 (principal to plan, plan and role to policy, policy to first enabled candidate, fallback chain, then `NoEligibleModel`), building the human-readable `reason` as it goes; verify unit tests over fixture catalog and policy data cover the first candidate winning, a disabled first candidate skipped with the skip recorded in the reason, a `None` principal resolving only `free_default`, and stable resolution across repeated calls.
- [x] 28.2 Implement the entitlement checks — plan derived only from `user_plans` keyed by the validated auth subject, `admin_experimental` gated on the administrative role rather than on any plan row, and every client-supplied plan, policy, model, entitlement, or role field ignored; verify tests cover a Free-plan caller with a body field claiming Pro resolving Free, a plan row that names `admin_experimental` still not resolving it for a non-administrative principal, and a direct API call producing the same outcome as one through the frontend.
- [x] 28.3 Implement the administrative override — validated against the database catalog before anything else, refused with `ModelNotAllowlisted` when the model is absent or disabled, recorded with the overriding principal, and never applied to another principal's concurrent run; verify tests cover an accepted override, an off-catalog refusal with no gateway call made, a disabled-model refusal, a concurrent non-administrative run unaffected, and a non-administrative caller's override field ignored without failing the request.
- [x] 28.4 Implement the fallback chain and its refusal — the policy's declared fallback, then the plan's default policy, then `LLM_MODEL` validated against the catalog, then `NoEligibleModel`, never escalating to a stronger policy's model; verify tests cover each rung of the chain, the Free-plan caller not receiving a stronger policy's available model, and `NoEligibleModel` surfacing as a configuration error with no answer produced.
- [x] 28.5 Implement `LLM_SINGLE_MODEL_MODE` and the settings validator refusing it in a deployed environment, plus the policy-store-unavailable path falling back to the configured model and recording that resolution was unavailable; verify tests cover development mode recording the configured fallback rather than a resolved policy, the validator refusing the flag in a deployed configuration, and the store-unavailable path.
- [x] 28.6 Implement `agents/llm/factory.py` building an `LLMClient` for a `Resolution` — per-instance `model_id`, one client per resolved model per call role, no module outside the factory constructing a gateway client; verify tests assert two call roles in one run may hold two different `model_id` values on the same `provider_id`, and that a test fails if a node constructs a client itself.
- [x] 28.7 Wire resolution into the graph — the supervisor requesting a `ROUTING` client and the synthesis node a `SYNTHESIS` client, each through the resolver, with the resolution's policy identifier, model identifier, reason, override and fallback indicators recorded into the evidence record and reported in the response envelope; verify fake-LLM tests assert no node names a model, that a run's evidence record states the resolution for each call, and that a test fails if `agents/nodes/` imports `entitlements/`.
- [x] 28.8 Add the behavior-invariance suite — the same question over the same fixture data under two policies resolving two different models produces identical tool selection, identical figures, an equally complete evidence record, identical grounding outcomes, and the same stream event sequence, differing only in prose and recorded model identity; verify the suite passes and that no policy record carries a field capable of disabling grounding, attribution, data-class labelling, or evidence capture.
- [x] 28.9 Implement the runtime infrastructure failover loop of `specs/model-policy` — entered only on a model-unavailable, provider-error, timeout or network outcome, walking the remaining enabled candidates of the same resolved policy in declared order, bounded by `LLM_FAILOVER_MAX_MODELS`, never escalating above the caller's entitlement, never triggered by a gateway rate limit where the candidates share a gateway account, and never triggered by any judgement of output quality; verify tests cover failover on 404, 5xx and timeout, no failover on 429, no failover on invalid output, a Free-plan caller never reaching a Pro or Premium candidate, the bound respected, and every attempt recorded with its selected model, outcome and reason.
- [x] 28.10 Implement fixed-model evaluation resolution — a live evaluation run resolving the fixed-model evaluation policy for every call role without reading the evaluation test user's plan, with failover disabled and no cross-model substitution for any reason; verify tests cover a live run pinning one model across every case, a plan or policy change leaving the pinned model unchanged, and a provider failure aborting the run rather than substituting a candidate.

## 29. LLM telemetry and cost estimation

- [x] 29.1 Implement `telemetry/usage.py` — the usage-event sink writing one `llm_usage_events` row per call attempt with every field `specs/llm-telemetry` requires, a null user identifier for a principal-less call rather than a placeholder, and the run and request correlation identifiers; verify `db` tests assert a complete row for a successful call, a null user identifier for an unauthenticated call, and that a run's events are listable from its run identifier.
- [x] 29.2 Implement `telemetry/cost.py` — the pure estimator over token counts and the catalog price row, returning null when either is unknown, copying the price and pricing date onto the event so re-pricing never rewrites history; verify unit tests cover a known computation, a null result for unknown tokens rather than zero, and a catalog re-pricing leaving past events unchanged.
- [x] 29.3 Implement `agents/llm/instrumented.py` — the `InstrumentedLLMClient` wrapper satisfying the same `LLMClient` Protocol, carrying the `CallContext` of design.md decision 24, and timing the gateway call only so the recorded latency excludes the telemetry write; verify tests assert the wrapper satisfies the Protocol, that latency measures the inner call, and that every path invoking a client does so through the wrapper.
- [x] 29.4 Implement failure classification and retry events — timeout, gateway rate limit, transport, authentication or configuration, schema validation, and unclassified — with token fields null rather than zero when the gateway reported none, and a retry emitting its own event carrying the attempt number and the identifier of the event it retried; verify respx and fake-LLM tests cover each classification, a malformed-JSON retry producing a failure event followed by a success event correctly linked, and null token fields on a failed call.
- [x] 29.5 Implement the bounded fire-and-forget write — a background task with its own short timeout and session, a logged counter on failure, and no propagation to the request; verify tests assert the caller still receives the answer when the telemetry write fails, that requests continue to be served while the telemetry store is unavailable, and that the answer, evidence record, and stream event sequence are identical with recording available and unavailable.
- [x] 29.6 Add the metadata-only assertion suite — no stored event or telemetry log line contains prompt text, completion text, retrieved passage text, an access token, an API key, or any other credential material, and diagnosis proceeds through the run identifier to the evidence record; verify the suite passes and fails when content is added to an event.
- [x] 29.7 Implement `telemetry/aggregate.py` — aggregation by model, policy, plan, call role, status, and period reporting call count, token totals, estimated cost total, failure rate, and median and 95th-percentile latency, splitting internal from product usage by the generated classification and returning aggregates rather than rows; verify `db` tests assert the measures are correct over seeded events, that internal events are reported separately and never counted against a plan, and that the aggregate over the retention window returns within the documented response budget.
- [x] 29.8 Extend the existing retention routine with `LLM_USAGE_RETENTION_DAYS` — raw events aged out or replaced by retained aggregates, a user's raw events removed with their account data, and non-attributable aggregates retained; verify `db` tests cover retention removing expired raw events, account deletion removing that user's events and consumption counters, and the routine remaining a single scheduled invocation rather than a second scheduler.
- [x] 29.9 Project the recorded inference attempts onto `llm_usage_events` rather than deriving a second, parallel record — the same stage, attempt number, outcome classification, provider, selected and served model, provider status and latency, enriched with the resolution's policy, catalog key and plan; verify tests assert one event per recorded attempt, that the event's classification equals the attempt's, and that no field is computed twice from different sources.

## 30. Usage limits and quota enforcement

- [x] 30.1 Implement `entitlements/quotas.py` — the allowance repository over `usage_limits` and the window-key derivation from `QUOTA_WINDOW_TIMEZONE` for the calendar day and calendar month, with a plan able to leave any dimension unlimited and the allowance model able to express an estimated-cost budget without restructuring; verify unit tests cover day and month key derivation across a boundary in the configured zone, an unset dimension not binding, and a cost-budget allowance being representable and enforceable by the same mechanism.
- [x] 30.2 Implement the atomic reserve-and-increment of design.md decision 25 for the request dimensions, with release on a failure before any gateway call, and the most restrictive binding dimension determining and naming the outcome; verify `db` tests cover admission within allowance, refusal when exhausted naming the bound dimension, a caller with one remaining request issuing several concurrently having exactly one admitted, and a reservation released when the request fails early.
- [x] 30.3 Implement the monthly token dimension as a pre-check against consumed totals settled from the recorded usage events afterward, with the bounded single-request overshoot documented; verify `db` tests cover refusal once the token allowance is reached, settlement from events, and reconciliation of counters against events for a window agreeing on both requests and tokens.
- [x] 30.4 Implement the concurrency dimension — incremented on run start and decremented in a `finally` so an aborted or errored run releases it; verify tests cover refusal at the concurrency limit naming the dimension, release after a successful run, and release after a run that raised.
- [x] 30.5 Implement the internal subject accounting — administrative, model-lab, and evaluation traffic accounted against the internal allowance by construction rather than by a flag, never consuming or attributed to any end user's plan, and refused with the quota error when the internal allowance is exhausted; verify `db` tests cover a lab run and an evaluation run leaving every product plan's consumption unchanged, an exhausted internal allowance refusing internal calls while product traffic continues, and internal usage reported separately per period.
- [x] 30.6 Implement the quota gate on the agent ask and streaming paths with the `QuotaExceeded` to HTTP 429 mapping carrying the bound dimension, allowance, consumption, reset time and retry-after, distinguishable from a 401, an upstream failure, an unconfigured agent, and a gateway 429; verify tests cover a refused question before any gateway call is made, a stream refused before it opens rather than terminated mid-answer, the error codes being distinguishable, and locations, current weather, forecast, history, analysis, comparison, and knowledge retrieval all still succeeding for a caller whose allowance is exhausted.
- [x] 30.7 Implement the protected own-plan-and-usage endpoint returning the caller's plan code and display name, per-dimension consumption and remaining allowance, each window's reset time, and a bounded recent summary, ignoring any caller-supplied user identifier and exposing no internal usage, no other user's usage, and no cross-user cost; verify tests cover the authenticated read, a supplied foreign identifier being ignored, an unauthenticated call refused, and no internal usage appearing.
- [x] 30.8 Implement the fail-closed behavior for a bounded dimension when the accounting store is unreadable or unwritable, the operator report on that path, and `QUOTA_ENABLED` for local and CI runs; verify tests cover a refusal rather than an unaccounted admission on store failure, the operator report emitted, and the suite running with enforcement disabled.
- [x] 30.9 Implement the thread-and-preferences-intact guarantee on a quota refusal and the mid-window allowance change semantics — applying to the remainder of the window against consumption already recorded and never re-evaluating requests already served; verify `db` tests assert a refused caller's thread, memory, preferences and saved locations are unchanged, and that raising an allowance mid-window admits the caller without re-evaluating served requests.

## 31. Administrative role and the administrative API surface

- [x] 31.1 Implement `auth/roles.py` and the administrative dependency — the role held as backend state keyed by the validated auth subject, never granted by a body field, query parameter, header, cookie, or unverified claim, and granting no access to any other user's own data; verify tests cover the role read from backend state, an asserted role ignored, every administrative capability refused without the role, and an administrative principal refused another user's threads, memory, preferences, saved locations, and evidence records exactly as any caller would be.
- [x] 31.2 Implement the `admin_audit` recorder capturing the acting principal, the action, the affected subject, the before and after values, and the timestamp for every privileged write across catalog, policy, plan, allowance, assignment, and promotion; verify `db` tests assert a record is written for each privileged write type and that the record names the acting principal.
- [x] 31.3 Implement the administrative model router — catalog listing filtered by status and capability role, catalog create, edit, enable and disable, policy listing and administration, and plan-to-policy mapping administration — under the versioned prefix with the standard error model; verify tests cover each operation applied and reflected in subsequent resolutions, a non-administrative caller refused with nothing disclosed, and an unauthenticated call refused.
- [x] 31.4 Implement the administrative plan router — plan listing and administration, allowance administration including the internal allowance, and a principal's plan assignment; verify tests cover an assignment taking effect for subsequent requests, an allowance change taking effect without a code change, an invalid allowance refused, a non-administrative caller refused, and the audit record written.
- [x] 31.5 Implement the administrative usage router returning aggregate usage by model, policy, plan, call role, status, and period with internal usage separated and no rows returned; verify tests cover the aggregate contents, internal separation, a non-administrative caller refused with no usage content returned, and no conversation content present in any response.
- [x] 31.6 Extend the published endpoint classification and the OpenAPI schema so every administrative model, plan, usage, and lab endpoint is present and classified as protected and administrative, and record the classification in `docs/api.md` and `docs/authentication.md`; verify a test asserts every administrative route appears in the classification and in the schema, and that the documented classification matches the routes actually registered.
- [x] 31.7 Add the administrative authorization suite — every administrative endpoint refused for an unauthenticated caller and for an ordinary authenticated caller with nothing disclosed about its contents, a second administrator seeing no other user's own data, and no administrative path reaching a user-owned row through the privileged connection; verify every case passes.

## 32. Model lab and model-policy evaluation

- [x] 32.1 Implement `lab/compare.py` driving the existing evaluation runner with the candidate model set as the varying axis and dataset version, weather fixtures, corpus, embedding model, analytics and commit pinned once for the whole comparison, over both a named dataset subset and a single ad-hoc question whose retrieval is performed once and replayed to every candidate; verify tests assert every candidate saw the same question and the same retrieved data, that every reported figure came from the same deterministic computation, and that the run record states the pinned configuration once.
- [x] 32.2 Implement `lab/records.py` persisting `model_comparison_runs` and `model_comparison_results` — per model per case: catalog reference and gateway model identifier, policy or lab context, latency, prompt, completion and total tokens, estimated cost, success or failure with its classification, the evaluation result where scored, and references to the usage events and, where the agent path ran, the agent run and evidence record — plus run provenance naming the initiating principal, time, dataset or question, catalog state and commit; verify `db` tests assert a complete result record, its links to telemetry and evidence, run provenance, comparability across runs, and readability after the model is disabled.
- [x] 32.3 Implement the lab bounds — `MODEL_LAB_MAX_MODELS`, `MODEL_LAB_MAX_CASES`, `MODEL_LAB_TIME_BUDGET_SECONDS` — refusing a request exceeding a bound and returning a partial result naming what completed when the wall-clock budget is reached, and continuing past a single model's failure rather than aborting; verify tests cover each bound refusal, a partial result on budget exhaustion, and one model timing out while the others still produce results.
- [x] 32.4 Add the lab isolation suite — the lab runs under the request-serving restricted role with the administrator's own principal, reads no other user's question, thread, memory, preference, saved location, evidence record or usage event, uses only the dataset, a fixture or the administrator's own data, writes into no other user's state, and alters no end user's plan, allowance or consumption; verify every case passes and that a test fails if the lab is given a privileged session.
- [x] 32.5 Implement the administrative lab router — initiating a comparison over selected enabled models, refusing a selection naming an absent or disabled model with no gateway call made, and reading a run and its per-model results; verify tests cover an initiated and read comparison, an off-allowlist selection refused, a non-administrative caller refused with no model identifier disclosed, and lab calls emitting internal usage events attributed to the initiating principal.
- [x] 32.6 Implement `evaluation/model_compare.py` — the runner accepting a candidate model set or a policy whose candidates are to be evaluated, producing one scored result per model over the selected cases using the existing dataset, metric definitions and thresholds unchanged; verify tests assert the existing metrics are computed unchanged, that only the model varies across candidates, and that a failing candidate does not abort the comparison.
- [x] 32.7 Implement the model-selection criteria of `specs/evaluation` — structured JSON reliability from the first-attempt-valid proportion and mean attempts to a valid decision, groundedness with the hallucination and unsupported-claim rates, latency as median and 95th percentile per call role, tool and planning quality including multi-step plan and ordering correctness, and cost from recorded tokens and catalog pricing labelled an estimate; verify tests assert all five criteria are reported for every candidate, that plan correctness is recorded per multi-step case, and that numerical calculation accuracy is 100% for every candidate over the same fixtures.
- [x] 32.8 Implement `model_evaluations` persistence and comparison — per model per run with the model, policy context, dataset version, every metric and criterion value, per-case outcomes, the recorded usage and the commit; comparable across runs and models, readable after a model is retired, and flagging a differing dataset version; verify `db` tests cover persistence, a model-to-model comparison, a run-to-run comparison, readability after retirement, and the dataset-version flag.
- [x] 32.9 Implement the promotion action — an explicit, separately authorized administrative write into a policy's candidate list recorded in `admin_audit` with the cited comparison run identifiers, refused for a model not enabled in the catalog, and refused on structured JSON reliability or groundedness grounds where the criteria were failed; verify tests assert a lab run alone leaves policy records, catalog statuses and plan mappings unchanged with concurrent product requests resolving exactly as before, that promotion is a separate authorized write recorded with its cited runs, that promoting a disabled model is refused, and that a cheaper faster candidate failing reliability or groundedness is not promoted on cost or latency alone.
- [x] 32.10 Wire evaluation runs into internal accounting — a live run emitting one internal usage event per call attributed to the run rather than to any end user, refused with the quota error when the internal allowance is exhausted, and an offline run making no gateway call, consuming no allowance and recorded as offline; verify tests cover live-run internal accounting leaving product plans unchanged, refusal on an exhausted internal allowance with product traffic unaffected, and an offline run consuming nothing and leaving every end user's threads, memory, preferences, saved locations, plan and consumption untouched.

## 33. Visily design and frontend for the model policy surfaces

The two Visily artifacts arrived on 2026-09-09 and are recorded and approved —
`docs/design/screens/09-admin-model-ai-usage.png` and `10-plan-usage.png`, with their per-screen
refusals in `docs/design/screens.md` §5 and the approval in §6. Approval is a gate on
implementation, not a decision to implement: both screens stay post-MVP per Part B, their routes
say so, and the administrative route issues no request at all.

Two readings are recorded rather than assumed, because a reviewer could reasonably ask about
either. **State coverage:** each artifact draws the populated state, and the loading, empty, error
and not-permitted treatments are `design-system.md` §11's approved shared patterns applied to that
composition — the same basis on which one authentication export serves eight screens and their
states (`screens.md` §3). **Element coverage:** `09` composes every element `specs/web-ui` names,
but under-draws two figures the spec fixes — a 95th-percentile latency beside the median, latency
and errors split by call role, and a list of past comparison runs beside the one its lab panel
draws. Those are recorded in `screens.md` §5's gap table as binding additions rather than waived,
which is how this project has handled an artifact that under-draws a required element since the
four-item sidebar.

One exception is recorded against 33.6: it asks for "a premium control hidden for an unentitled
plan", and no such control exists anywhere on the MVP agent surfaces — `AskRequest` carries a
question, units and a thread and forbids extra fields, and the frontend holds no plan or
entitlement value at all. Rather than invent one, the property behind the requirement is asserted
in its strongest available form: the request body cannot carry a model, plan, policy or entitlement
field, so there is nothing for a hidden control to gate and no way for the UI to decide which model
serves a request. The scenario becomes live if such a control is ever added, and the assertion is
written to fail then.

- [x] 33.1 Produce the Admin Model & AI Usage screen design in Visily — model status with its enable and disable controls, token usage, estimated cost, latency, errors, plan usage with internal usage separated, and the internal model selector with past comparison results — covering the populated, loading, empty, error and not-permitted states and following the established Weathra design system; verify the recorded artifact covers every element and state `specs/web-ui` names for the screen and is referenced by its implementation entry.
- [x] 33.2 Produce the Plan & Usage view design in Visily — plan name, per-dimension consumption and remaining allowance, and window reset times — covering the populated, loading, empty and error states; verify the recorded artifact covers each state and shows no other person's usage, no internal usage and no cross-user cost.
- [x] 33.3 Record both screens in the design roadmap as post-MVP entries and record their approval as the implementation reference before any implementation begins; verify `docs/design/` and `docs/roadmap.md` agree on the classification and that no route advertises either screen as working while it is unbuilt.
- [x] 33.4 Implement the post-MVP route stubs for both screens stating they are not yet available, with the administrative route fetching no catalog, usage, cost or lab content for a non-administrative visitor; verify component tests assert each stub renders its not-yet-available state and that an ordinary authenticated person navigating to the administrative route triggers no administrative request.
- [x] 33.5 Implement the quota-limit state across the MVP agent surfaces — the AI Weather Analyst and the Dashboard presenting an exhausted allowance as a distinct, honest state naming the limit and its reset time, visually and textually separate from a weather error and from an expired session, with the person's thread, saved locations and preferences left intact; verify component tests cover a 429 producing the quota state rather than a data or session error, the limit and reset time being shown, and the person's saved data remaining available.
- [x] 33.6 Implement the display of what actually served an answer — the provider, model and resolving policy reported by the backend shown as returned rather than assumed client-side, and a premium control hidden for an unentitled plan reflecting whatever the backend actually did when the underlying request is issued anyway; verify component tests assert the reported values are rendered from the response, that the UI never decides which model serves a request, and that a hidden control is not the gate.

## 34. SaaS-layer documentation and acceptance verification

- [x] 34.1 Write `docs/model-policy.md` — the resolution order, the shipped policies and their candidate lists, the plan-to-policy mapping, the entitlement rules, the administrative override and its allowlist bound, the fallback chain, the role of `LLM_MODEL`, and the catalog staleness window with the honest statement that a disable is not instant; verify a reader following it can predict which model a given plan and call role resolves and can name what happens when nothing is available.
- [x] 34.2 Extend `docs/configuration.md`, `backend/.env.example`, and the deployment notes with the new backend variables of design.md decision 19, and state explicitly that policies, plan mappings, allowances and the catalog are database rows rather than environment variables so a model or tier change needs no redeployment; verify a test asserts every new setting appears in `.env.example` and that no new secret carries a `NEXT_PUBLIC_` prefix.
- [x] 34.3 Extend `docs/privacy-ethics.md` with the telemetry stance — metadata only, no prompt or completion text, owner-scoped events, aggregates that disclose no content, the retention window, and what account deletion removes — and `docs/authentication.md` with the administrative role and the SaaS-ready table classification; verify both documents match the implemented behavior and the RLS classification actually applied by migration.
- [x] 34.4 Extend `docs/evaluation.md` with the model-comparison methodology — the five selection criteria and how each is measured, what is pinned across candidates, why numerical accuracy is model-independent, and the rule that reliability and groundedness cannot be traded away for cost or latency; verify the document states each criterion's measurement definition and how to run a comparison.
- [ ] 34.5 Execute the first model comparison across the seeded catalog candidates, record its results, and seed or reorder the policy candidate lists from the recorded evidence with the promotion audited against the cited runs; verify the comparison record exists with all five criteria per candidate, the resulting policy state cites it, and `docs/evaluation.md` records the outcome honestly including any candidate that failed a criterion.
- [x] 34.6 Extend the traceability table in `docs/architecture.md` to cover `model-policy`, `model-catalog`, `llm-telemetry`, `usage-limits`, and `model-lab`; verify every requirement in those five specs maps to at least one test.
- [x] 34.8 Implement the administrative model policy confirmation surface `specs/web-ui` requires — on the administrative route, reachable only by a principal the backend confirms holds the role, presenting each policy's ordered candidate list with the evaluation the backend recorded per candidate, an unevidenced candidate marked unevidenced rather than failed, the comparison runs available as evidence, and one audited write that submits the candidate list citing the runs relied upon, with the recorded result read back from the audit trail rather than assumed; add the smallest backend read that exposes one policy's audit trail, and keep the screen's unbuilt panels stating that they are unbuilt and fetching nothing; verify a non-administrator is shown a not-permitted state with nothing behind it, that the write sends the stored order and the selected run and nothing else, that a promotion-gate refusal is shown as itself, and that no credential is displayed or read outside the API client.
- [x] 34.9 Report the acting principal's administrative capability on the authenticated account contract, read from the same `admin_roles` state every administrative capability checks and answering only for the validated token subject, and offer an Admin section in the navigation to a principal the backend confirms holds the role — naming only administrative surfaces that are implemented, defaulting to offering nothing where the capability is unknown, and never inferring it from an address, an identifier list, a configuration value or a stored flag; verify an ordinary person is offered nothing, an administrator is offered a real link to the route, a token asserting the role changes nothing, and the route still refuses a principal without it.
- [x] 34.10 Implement Plan & Usage against `GET /me/usage` and `docs/design/screens/10-plan-usage.png` — the plan in effect among the plans Weathra has, consumption against allowance per dimension with its reset window, the recent-activity summary, and a pointer at any dimension near or over its allowance — refusing the artifact's billing half, its vector-node and rate-limiter controls and its per-user usage export, and offering it in the navigation as an account destination; verify an unlimited dimension reads as unlimited rather than as a proportion, an absent token count reads as unreported rather than zero, a concurrency dimension is given no fabricated reset, an internal principal is told their calls do not spend the plan, and no purchase or payment control appears.
- [x] 34.11 Recover the saved locations already stored with no canonical name — present a place whose only name is its coordinates as unnamed rather than as digits, keep the coordinates as where it is, and let its owner name it through the ordinary save so the label updates the existing place rather than duplicating or moving it and nobody removes and re-adds anything; do not substitute a nearby named place, since the geocoder resolves names to coordinates and not the reverse and the only name-shaped field a coordinate lookup returns is a timezone whose city may be a thousand kilometres away; verify the rule holds for arbitrary worldwide points from the shape of the stored name alone, with no city named in the implementation.
- [x] 34.12 Group the navigation by what a screen is for — the seven core screens unheaded, then the five intelligence screens, then the account group, then the administrative group for a principal the backend confirms holds the role — and replace the PLANNED badge beside an unbuilt intelligence screen with a de-emphasised row whose accessible name carries the caveat, removing that caveat per screen as each is built and never before; verify the four groups render in order, an unbuilt entry is dimmed and caveated without a badge, a built one carries neither, and the administrative group stays absent for everybody else.
- [x] 34.13 Build Forecast Explorer on `/weather/current`, `/weather/forecast` and `/weather/analysis` — the metric row, a selectable horizon, the forecast series charted, the deterministic findings and the forecast's own confidence beside it, and every hourly entry the provider returned as a table — replacing the artifact's named forecast model, sensor nodes, convergence and alignment percentages, reliability score and encryption banner with the provider that actually answered; verify a chosen horizon re-requests, an absent hourly series is stated rather than drawn, no entry is interpolated to fill a grid, and no language model is called.
- [x] 34.14 Build the Weather Intelligence Report over `/weather/current`, `/weather/forecast`, `/weather/changes`, `/weather/analysis` and `/weather/history/baseline`, offering the model-written synthesis as a control rather than spending a model call on navigation, and omitting the artifact's agent version, evidence-node count and PDF export; verify the report renders complete with no model called, and that asking for the synthesis names the model and offers its evidence record.
- [x] 34.15 Build Travel Intelligence over `POST /weather/comparison`'s day-level ranking, showing each day's rank, its supporting statistics and the contributions behind its score, and computing no index of its own; replace the artifact's flight stability, airline operations, booking and sensor telemetry with a stated limit to weather; verify the ranking is requested only when asked, the backend's own figures are what appear, and no aviation or booking content is present.
- [x] 34.16 Implement the deterministic scenario capability — `analytics/scenario.py` applying stated assumptions to a retrieved series with physical bounds honoured and crossings counted, `POST /weather/scenario` returning the unmodified baseline beside the adjusted series with the arithmetic per measure and a disclaimer, and the Weather Scenario Lab screen over it — computing nothing in the browser, calling no language model, and labelling the result simulated throughout; verify an absent value stays absent, a bound is applied and counted, the result is deterministic, and none of the artifact's simulation engine, inference model or node counts appears.
- [x] 34.17 Implement Weather Watch — an additive `weather_watches` migration with the same forced owner-only RLS every user-owned table carries, an owner-scoped store, five routes for listing, creating, changing, removing and checking a watch, deterministic threshold evaluation with no model involved, and the screen over them — evaluating on view and on request because there is no scheduler, saying so in the payload as well as on screen, and recording an absent reading as no answer rather than as the condition failing; verify two people cannot see or change each other's watches, the same question asked twice updates one watch, an evaluation records its moment, and no monitoring or alerting is claimed.
- [x] 34.18 Build the Admin Model & AI Usage dashboard over the administrative reads that already exist — `GET /admin/usage`'s grouped aggregates and `GET /admin/models`' catalog with its recorded evaluations — composing the artifact's KPI row, usage chart, grouped table, product-and-internal split, failures panel and model table from measures the backend actually returns; treat an unreported token or cost figure as unreported rather than as zero, report the highest per-group median as exactly that rather than averaging medians, and write no model name into the frontend at all; verify a caller without the administrative role is refused with no figures behind the refusal and no invitation to retry the same question, that the route never reads the acting person's own plan or consumption, and that none of the artifact's fictional model rows, composite scores, per-tier headcounts or operational-status claims appears.
- [ ] 34.7 Run the SaaS-layer acceptance verification end to end against the deployed backend — a Free-plan caller served their entitled model with the resolution recorded in the evidence record; a body field claiming Pro ignored; an administrative override accepted for an enabled model and refused for a disabled one; a usage event recorded for every call including a forced failure; an estimated cost present and labelled; an exhausted allowance returning 429 with its basis while forecast, history, analysis and comparison still serve; internal lab and evaluation usage reported separately from every product plan; a second account seeing none of the first's usage; and both memory tiers plus every deterministic figure unchanged throughout; verify each and record the results.

# Part B — Post-MVP / advanced SaaS capabilities

Deliberately **not** tracked as tasks in this change and **not** to be implemented by the apply phase. Recorded so the architecture keeps room for them and `docs/roadmap.md` stays honest.

**Identity and access (beyond the MVP's Supabase Auth email/password)**

- Enterprise single sign-on (SAML/OIDC).
- Additional OAuth identity providers.
- Multi-factor authentication.
- Organizations, teams, and role-based access control.
- Multi-tenancy, rate limiting, and API keys for third-party consumers.
- Rate limiting on the public weather endpoints, needed before wide public exposure.
- Graded roles, scopes, and organization-level administration beyond the single administrative/internal flag of group 31.

**Product capabilities**

- Historical forecast-accuracy and skill scoring — compare stored forecast snapshots against subsequent observations; needs a long snapshot history and scheduled capture.
- Weather Scenario Lab — explore hypothetical or alternative conditions and their implications.
- Travel Intelligence — weather intelligence along a route or across trip dates.
- Weather Watch — monitor a location against conditions and notify on change; needs scheduling and notification infrastructure.
- Weather Intelligence Report — a composed, exportable report across capabilities.
- Behavioral personalization — intelligence shaped by usage history rather than only explicit preferences.
- Multi-provider forecast consensus and a confidence signal derived from provider disagreement.

**Screens**

- Weather Intelligence Report, Forecast Explorer, Weather Scenario Lab, Weather Watch, Travel Intelligence — represented in the Visily design roadmap, not implemented in this change.
- Admin Model & AI Usage and Plan & Usage — designed and approved in group 33 and represented in the design roadmap; implemented post-MVP.

**Commercial and model governance**

- Payment processing — checkout, card handling, invoicing, dunning, proration, and a billing-provider integration. Plans are administratively assigned in this change; `subscription_plans` carries a stable plan code and an unused external subscription reference so the integration has somewhere to land.
- Enforced estimated-cost budgets per plan and window against paid models. The allowance model of group 30 can express one; none is enabled.
- Reconciliation of estimated cost against a gateway invoice. Cost stays an operational estimate in this change.
- Self-service plan upgrade and downgrade, which needs payment processing first.
- Automatic or adaptive model routing — bandits, per-question difficulty routing, or cost-aware fallback chosen at runtime. Policies stay declared candidate lists in a fixed order, promoted by a recorded human decision.
- Scheduled model health probing and automatic disabling of a failing catalog entry.
- Per-model prompt variants, so a policy could carry a prompt tuned to its model rather than sharing one.

**Platform and architecture**

- Scheduled forecast snapshot capture, improving What Changed? coverage beyond request-driven snapshots.
- Additional weather providers behind the existing provider contract.
- Additional LLM adapters behind the existing client contract, including a native tool-calling route for models that support it reliably.
- A shared cache replacing the process-local one, behind the existing `CachedProvider` seam.
- Promoting the MCP server to its own deployed service.
- ChromaDB as an alternative vector store behind the existing retrieval interface.
- Push notifications and severe-weather alerting, subject to the official-warnings stance in `specs/safety-grounding`.
- Infrastructure-as-code for the deployment topology.
